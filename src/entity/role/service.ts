import service from "../../core/service";

export default class roleService extends service {
  /**
   * constructor function for roleService.
   *
   * @remarks
   * This method is part of the roleService class extended of the main parent class baseService.
   *
   * @param model - roleSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}

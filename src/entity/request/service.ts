import service from "../../core/service";

export default class requestService extends service {
  /**
   * constructor function for requestService.
   *
   * @remarks
   * This method is part of the requestService class extended of the main parent class baseService.
   *
   * @param model - requestSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}

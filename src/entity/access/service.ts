import service from "../../core/service";

export default class accessService extends service {
  /**
   * constructor function for accessService.
   *
   * @remarks
   * This method is part of the accessService class extended of the main parent class baseService.
   *
   * @param model - accessSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}

import service from "../../core/service";

export default class logService extends service {
  /**
   * constructor function for logService.
   *
   * @remarks
   * This method is part of the logService class extended of the main parent class baseService.
   *
   * @param model - logSchema
   *
   * @beta
   */
  constructor(model: any) {
    super(model);
  }
}
